# Используйте официальный образ Golang в качестве базового образа
FROM golang:latest

# Установка рабочей директории внутри контейнера
WORKDIR /app

# Копирование файлов приложения в контейнер
COPY . .

# Сборка Go приложения
RUN go build -o main .

# Экспозиция портов, которые ваше приложение использует
EXPOSE 80
EXPOSE 8080

# Запуск вашего приложения при старте контейнера
CMD ["./main"]
